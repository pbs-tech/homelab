---
dependency:
  name: galaxy
  options:
    requirements-file: ../../requirements.yml

driver:
  name: docker

platforms:
  - name: ubuntu-test
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    capabilities:
      - SYS_ADMIN
    groups:
      - proxmox_lxc_test

provisioner:
  name: ansible
  config_options:
    defaults:
      callbacks_enabled: profile_tasks,timer,yaml
      stdout_callback: yaml
      host_key_checking: false
      remote_tmp: /tmp/.ansible-${USER}/tmp
  inventory:
    host_vars:
      ubuntu-test:
        ansible_user: root
        ansible_connection: docker
        # Override network settings for testing
        ansible_default_ipv4:
          address: 127.0.0.1
        # Mock Proxmox settings for testing
        proxmox_host: 127.0.0.1
        proxmox_user: root@pam
        container_ssh_key: ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABgQC... mock-key

verifier:
  name: ansible

scenario:
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - verify
    - cleanup
    - destroy
  # Note: Idempotence test skipped for service roles due to dynamic
  # configuration changes and state management in containerized services
