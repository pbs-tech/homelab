---
galaxy_info:
  author: homelab
  description: Deploy radarr service
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - jammy

dependencies:
  - role: homelab.common.container_base
    when: radarr_apply_container_base | default(false)
