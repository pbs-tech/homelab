http:
  routers:
{% for service_name, service_config in services.items() %}
    {{ service_name }}-router:
      rule: "Host(`{{ service_config.domain }}`)"
      service: {{ service_name }}-service
      tls:
        certResolver: {{ traefik_ssl_resolver }}
      middlewares:
        - default-headers
{% endfor %}

    # K3s cluster services router
    k3s-router:
      rule: "Host(`k3s.{{ homelab_domain }}`) || HostRegexp(`{subdomain:[a-z0-9-]+}.k3s.{{ homelab_domain }}`)"
      service: k3s-service
      tls:
        certResolver: {{ traefik_ssl_resolver }}
      middlewares:
        - default-headers

  services:
{% for service_name, service_config in services.items() %}
    {{ service_name }}-service:
      loadBalancer:
        servers:
          - url: "http://{{ service_config.ip }}:{{ service_config.port }}"
{% endfor %}

    # K3s cluster service (load balanced across nodes)
    k3s-service:
      loadBalancer:
        servers:
          - url: "https://192.168.0.111:6443"
          - url: "https://192.168.0.112:6443"
          - url: "https://192.168.0.113:6443"
          - url: "https://192.168.0.114:6443"

  middlewares:
    default-headers:
      headers:
        frameDeny: true
        sslRedirect: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 31536000
        customRequestHeaders:
          X-Forwarded-Proto: "https"

    secure-headers:
      headers:
        customRequestHeaders:
          X-Forwarded-Proto: "https"
        customResponseHeaders:
          X-Robots-Tag: "none,noarchive,nosnippet,notranslate,noimageindex"
        sslProxyHeaders:
          X-Forwarded-Proto: "https"