---
- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: Restart media-stack
  ansible.builtin.systemd:
    name: media-stack
    state: restarted
    daemon_reload: true
