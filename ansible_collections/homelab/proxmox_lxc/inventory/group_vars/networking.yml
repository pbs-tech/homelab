---
# WireGuard VPN server configuration
# Applied to the wireguard* host in the networking inventory group

# Public endpoint — the Proxmox host's public IP or DDNS hostname
wireguard_public_endpoint: "81.108.204.223"

# VPN clients authorised to connect
# Add one entry per device; remove REPLACE placeholders from defaults
wireguard_clients:
  - name: control-machine
    description: Arch Linux control machine
    public_key: "n2ivP54eS2XygydmVJWqloOtxthDj+Sav9sUwGW3LAU="
    allowed_ips: 10.200.0.2/32
    preshared_key: "{{ vault_wireguard_client_preshared_key_control_machine }}"
    persistent_keepalive: 25
