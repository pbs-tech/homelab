---
galaxy_info:
  author: "Homelab Administrator"
  description: "Deploys and configures K3s agent (worker) nodes that join an existing K3s cluster"
  company: "Homelab"
  license:
    - "Apache-2.0"
  min_ansible_version: "2.12"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - focal
    - name: Debian
      versions:
        - bullseye
        - bookworm
    - name: EL
      versions:
        - 8
        - 9
  galaxy_tags:
    - k3s
    - kubernetes
    - cluster
    - agent
    - worker
    - node
    - raspberrypi
    - homelab

dependencies:
  - role: homelab.k3s.prereq
