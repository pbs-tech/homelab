---
galaxy_info:
  author: "Homelab Administrator"
  description: "Deploys and configures K3s server (control plane) nodes with support for single-server and HA multi-server deployments"
  company: "Homelab"
  license: "Apache-2.0"
  min_ansible_version: "2.12"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - focal
    - name: Debian
      versions:
        - bullseye
        - bookworm
    - name: EL
      versions:
        - all
  galaxy_tags:
    - k3s
    - kubernetes
    - cluster
    - server
    - controlplane
    - raspberrypi
    - homelab
    - ha
    - etcd

dependencies:
  - role: homelab.k3s.prereq
