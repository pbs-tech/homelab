---
galaxy_info:
  author: "Homelab Administrator"
  description: >
    Core role for Proxmox KVM/QEMU virtual machine lifecycle management,
    providing standardized VM creation, configuration, and secure API
    integration for homelab infrastructure deployments.
  company: Homelab
  license: Apache-2.0
  min_ansible_version: "2.12"

  platforms:
    - name: Ubuntu
      versions:
        - focal      # 20.04 LTS
        - jammy      # 22.04 LTS
    - name: Debian
      versions:
        - bullseye   # 11
        - bookworm   # 12

  galaxy_tags:
    - homelab
    - proxmox
    - kvm
    - qemu
    - virtualization
    - infrastructure
    - automation
    - api
    - deployment

# Collection dependencies: community.proxmox (>=1.3.0)
# These are declared in requirements.yml at the collection level
dependencies: []

allow_duplicates: false
