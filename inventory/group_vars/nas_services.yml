---
# The nas_services group is now empty — all media services run on the media-stack VM.
# This file is retained to avoid "undefined group_vars" warnings.
# See: ansible_collections/homelab/proxmox_lxc/roles/media_stack/
