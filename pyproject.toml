[project]
name = "homelab"
version = "0.1.0"
description = "Ansible homelab infrastructure collections for Proxmox LXC, K3s, and common roles"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
